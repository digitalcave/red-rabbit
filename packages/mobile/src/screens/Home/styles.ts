import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  button: {
    padding: 16,
    borderRadius: 4,
  },
  image: {
    width: '100%',
    height: '100%',
  },
})

export default styles
